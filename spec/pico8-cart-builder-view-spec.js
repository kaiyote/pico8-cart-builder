'use babel';

import Pico8CartBuilderView from '../lib/pico8-cart-builder-view';

describe('Pico8CartBuilderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
